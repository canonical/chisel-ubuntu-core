package: sudo-common

essential:
  sudo-common_copyright:

slices:
  config:
    contents:
      /etc/pam.d/sudo:
      /etc/pam.d/sudo-i:
      /etc/sudoers:
      /etc/sudoers.d/: {make: true, mode: 0750}
      /var/lib/sudo/: {make: true, mode: 0755}

  copyright:
    contents:
      /usr/share/doc/sudo-common/copyright:
